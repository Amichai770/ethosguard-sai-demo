{
  "name": "ethosguard-sai-demo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html"
  },
  "engines": {
    "node": ">=18"
  }
}
